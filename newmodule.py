def house_area():
    length = input('What is the length? ')
    width = input('What is the width? ')
    
    area = int(length) * int(width)
    print(area)
    return area


house_area()

# not sure why second functions starts without being called

